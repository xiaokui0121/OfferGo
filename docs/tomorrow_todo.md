# 明天待办（5/13）

## 任务
1. **部署**（GitHub 托管 + Railway/Render + Anthropic API key）
2. **测试**（点完每个页面 + 跑通核心流程）
3. **写文档**（README + 演进路径）

## 文档里要写的「多用户演进路径」（用户原话提到的点）

当前是单用户 Demo 模式，所有数据共用一个数据库。要做多用户的话，需要：

- 接入邮箱 / OAuth 登录（Supabase Auth 或 Clerk）
- 数据表加 user_id 字段，API 鉴权 + 数据隔离
- 小红书采集会话按用户隔离 + 队列调度
- **当前架构里小红书登录态是全局共享的，多用户后是最大改造点**

不建议接微信登录——个人开发者拿不到资质，企业账号要走法务。
邮箱 + Google OAuth 入门更现实。

## 不要做的
- 不要在 demo 前真的实现多用户登录（面试官看 demo 不在乎这个）
- 不要碰微信登录
